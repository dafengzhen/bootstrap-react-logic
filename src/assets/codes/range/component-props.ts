const rangeComponentPropsCodes = {
  rangeComponentProps: {
    code: `
type Props = {
  /**
   * disabled.
   */
  disabled?: boolean;

  /**
   * min.
   */
  min?: string | number;

  /**
   * max.
   */
  max?: string | number;

  /**
   * step.
   */
  step?: string | number;
};
    `,
  },
};

export default rangeComponentPropsCodes;
